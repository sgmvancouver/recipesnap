# Netlify configuration for RecipeSnap

[build]
  publish = "dist"
  command = "npm run build"

# SPA fallback — all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
